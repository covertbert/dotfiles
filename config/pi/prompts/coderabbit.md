---
description: Review CodeRabbit findings on current branch GitLab merge request
argument-hint: "[dry-run|apply|push]"
---

Load and follow the `coderabbit-review` skill. Run it in `${1:-push}` mode against the open GitLab merge request tied to the current branch.

Additional instructions: ${@:2}
